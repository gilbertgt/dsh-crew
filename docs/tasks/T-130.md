# T-130 — 删 docs/qa/ + 最终验证

- **Verdicts**：code: not run — 删目录，不含产品代码 ｜ security: not run — 不动权限 ｜ qa: not run — 判据是 grep=0 + npm test 全绿 ｜ doc: not run — 任务进行中

- **里程碑**：M1 ｜ **形状**：单人（solo），**工程师做**
- **拥有的文件**：`docs/qa/`（删）
- **依赖**：T-127/T-128/T-129（外部引用已全部改到 qa/）

## 要做什么

删 `docs/qa/` 整个目录。之后 `grep -r "docs/qa" .` 应只剩 git 历史/CHANGELOG 提及。QA 套件从 `qa/` 跑。

## DoD（PM 写）

| # | 怎么算做完 | 别人怎么验 |
| --- | --- | --- |
| 1 | `docs/qa/` 不存在 | `ls docs/qa/` 报不存在 |
| 2 | `grep -r "docs/qa" .` 为 0（除 git 历史/CHANGELOG 提及） | grep |
| 3 | `npm test` 全绿，QA 套件从 `qa/` 跑 | `npm test` |
| 4 | 过一轮 review（code + doc） | review 报告 |
