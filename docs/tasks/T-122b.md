# T-122b — 24 个 QA case 的路径引用改指 docs/tasks/

- **Verdicts**：code: not run — 任务进行中 ｜ security: not run — 不动权限 ｜ qa: not run — 判据是各 case 单跑绿 + npm test ｜ doc: not run — 任务进行中

- **里程碑**：M1 ｜ **形状**：单人（solo），**工程师做**
- **拥有的文件**：引用 `docs/design/tasks.md` 的 24 个 `docs/qa/T-*/case-*.mjs`
- **依赖**：T-121、T-122a

## 要做什么

把 24 个 QA case 里对 `docs/design/tasks.md` 的路径引用改成 `docs/tasks/`（目录）或
`docs/tasks/T-<n>.md`。**只改路径，不改断言语义**——case 断言什么就断言什么，只是去哪里找。

## DoD

| # | 怎么算做完 | 别人怎么验 |
| --- | --- | --- |
| 1 | `grep -r "docs/design/tasks.md" docs/qa/T-*/case-*.mjs` 为 0（除历史提及） | grep |
| 2 | 改过的每个 case 单跑绿 | 逐个跑 |
| 3 | `bash docs/qa/run-all.sh` 里所有 T-* 绿（除已知的 T-67/T-79 状态由 PM 处理） | run-all |
