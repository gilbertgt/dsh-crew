# T-129 — roles/*.md + CLAUDE.md + principles.md + README 对 + docs/ 提及改 qa/

- **Verdicts**：code: not run — 任务进行中，评审在第 10 步 ｜ security: not run — 不动权限 ｜ qa: not run — 判据是 grep=0 + npm test ｜ doc: not run — 任务进行中

- **里程碑**：M1 ｜ **形状**：单人（solo），**工程师做**
- **拥有的文件**：`roles/*.md`、`CLAUDE.md`、`principles.md`、`README.md`、`README-zh.md`、`docs/**/*.md`（提及 docs/qa 的）
- **依赖**：T-126

## 要做什么

所有 `docs/qa` **路径提及**改 `qa/`（规则文字 CLAUDE.md 已由 PM 改好；这里是其余提及）。只改路径字符串，不改任何规则语义。

## DoD（PM 写）

| # | 怎么算做完 | 别人怎么验 |
| --- | --- | --- |
| 1 | `grep -r "docs/qa" roles/ CLAUDE.md principles.md README.md README-zh.md docs/` 为 0（除 git 历史/CHANGELOG 提及） | grep |
| 2 | 9 个角色提示仍被 verify-mount 接受 | `node tools/verify-mount.mjs` 绿 |
| 3 | `npm test` 绿（若 T-130 未删 docs/qa 则 QA 套件仍从 docs/qa 跑——记录） | `npm test` |
