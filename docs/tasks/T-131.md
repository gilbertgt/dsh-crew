# T-131 — qa/T-*/ 用例文件内的 docs/qa 引用改 qa/（含 T-52 的 REPO 深度）

- **Verdicts**：code: not run — 任务进行中，评审在第 10 步 ｜ security: not run — 不动权限 ｜ qa: not run — 判据是 grep=0 + qa/run-all.sh 绿 ｜ doc: not run — 任务进行中

- **里程碑**：M1 ｜ **形状**：单人（solo），**工程师做**
- **拥有的文件**：`qa/T-*/` 下所有引用 `docs/qa` 的 case/helper 文件（T-126 的 5 个文件除外）、`qa/T-52/principles.mjs`
- **依赖**：T-126（lib REPO 已修）；与 T-127/128/129 并行（不同文件）

## 要做什么

T-126 只修了 qa/ 的 5 个顶层文件。但 `qa/T-*/` 用例文件里还有 **231 处 `docs/qa` 引用**——部分是注释（可留历史），部分是**运行时路径**（`repoFile("docs/qa/gaps.md")`、`GAPS = "docs/qa/gaps.md"`、pin `bash docs/qa/run-all.sh` 等）——T-130 删 docs/qa 后它们会运行时崩。全部改成 `qa/`：
- 运行时路径（repoFile、常量、pin 字符串）必须改（否则删目录后崩）。
- 注释里的历史提及可留（T-130 的 grep 允许历史提及），但若注释描述的是"当前路径"则改。
- **T-52/principles.mjs**：自带 REPO 常量上 3 层（同 T-126 修的 bug）——改成上 2 层。

## DoD（PM 写）

| # | 怎么算做完 | 别人怎么验 |
| --- | --- | --- |
| 1 | `grep -rn "docs/qa" qa/T-*/` 为 0（除明确历史提及） | grep |
| 2 | `bash qa/run-all.sh` 全绿（无 REPO 深度/路径崩溃） | 跑 |
| 3 | 运行时路径（repoFile/pin/常量）全部指向 qa/ | 抽查 diff |
| 4 | T-52/principles.mjs REPO 指向仓库根 | 读常量 |
