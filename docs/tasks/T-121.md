# T-121 — 第 1 步：verify-tasks 改扫 docs/tasks/ 目录（目录已建，任务行已在此）

- **Verdicts**：code: not run — 任务进行中，评审在第 10 步 ｜ security: not run — 不触碰权限或命令路径 ｜ qa: not run — 判据是双源验证 + npm test ｜ doc: not run — 任务进行中

- **里程碑**：M1 ｜ **形状**：单人（solo），**工程师做**
- **拥有的文件**：`tools/verify-tasks.mjs`、`package.json`（如需要）
- **依赖**：PRD `docs/design/prd-2026-08-23-split-tasks-table.md`（版本 2）

## 要做什么

`docs/tasks/` 目录已由 PM 建好（含本作业 5 个任务行 T-121~T-125 + README.md）。本步把
`tools/verify-tasks.mjs` 从 `readFileSync("docs/design/tasks.md")` 改成**扫 `docs/tasks/*.md`**：
- 每个 `docs/tasks/T-<n>.md` 的顶部标题 `# T-<n> — ...` 是一个 task section，验证其 Verdicts 行；
- **目录为空 → 红**（防 vacuous green）。本目录现有 5 个任务行，不会为空。
- **过渡期**：同时验 tasks.md 和 docs/tasks/（两个都读），保证不丢覆盖；T-123 删 tasks.md 时去掉旧的一半。

## DoD（PM 写）

| # | 怎么算做完 | 别人怎么验 |
| --- | --- | --- |
| 1 | 扫 `docs/tasks/` 且对两处（tasks.md + 目录）都绿 | `node tools/verify-tasks.mjs` 输出两个来源的 section 数 |
| 2 | 目录里 5 个任务行被读到 | 输出含 `docs/tasks` 来源，5 个 section |
| 3 | 坏 Verdicts 仍红（变异证明） | 变异测试 |
| 4 | `npm test` 全绿 | `npm test` |
