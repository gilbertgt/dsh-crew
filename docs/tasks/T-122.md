# T-122 — 第 2 步：修引用（拆成 4 个并行子任务 a/b/c/d）

- **Verdicts**：code: not run — 任务进行中，评审在第 10 步 ｜ security: not run — 不动权限或命令路径 ｜ qa: not run — 判据是各子任务验证 + npm test ｜ doc: not run — 任务进行中

- **里程碑**：M1 ｜ **形状**：拆成 4 个并行 solo 子任务（T-122a/b/c/d），见下
- **拥有的文件**：由各子任务分别列出（互不重叠）
- **依赖**：T-121（verify-tasks 已改双源）

## 为什么拆

原来一个工程师扛四类活（lib、24 个 case、9 个角色提示、rule-guard-map），且与别的 agent
并行改同一棵树。拆成 4 个互不重叠的并行任务，各自可独立验证。

## 子任务

- **T-122a**：`docs/qa/lib/qa.mjs` —— `tempRepo()` 拷贝清单加 `docs/tasks/`；`TASKS_MD` 指向目录。
  **先做**（其他子任务依赖它：case 在拷贝里跑 verify-tasks 需要目录在）。
- **T-122b**：24 个 QA case 的路径引用（`docs/design/tasks.md` → `docs/tasks/` 或 `docs/tasks/T-<n>.md`）。
- **T-122c**：9 个角色提示的路径引用。
- **T-122d**：`docs/qa/rule-guard-map.md` 的 DoD 来源。

## DoD（PM 写，全拆完验收）

| # | 怎么算做完 | 别人怎么验 |
| --- | --- | --- |
| 1 | `grep -r "docs/design/tasks.md" docs/qa/ roles/` 为 0（除历史提及） | grep |
| 2 | 改过的每个 QA case 单跑绿（含 T-42） | 逐个跑 |
| 3 | 9 个角色提示仍被 verify-mount 接受 | `node tools/verify-mount.mjs` 绿 |
| 4 | `npm test` 全绿 | `npm test` |
