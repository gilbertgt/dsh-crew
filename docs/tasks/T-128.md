# T-128 — tools/verify-*.mjs 的 docs/qa 引用改 qa/

- **Verdicts**：code: not run — 任务进行中，评审在第 10 步 ｜ security: not run — 不动权限 ｜ qa: not run — 判据是各 verify 绿 + npm test ｜ doc: not run — 任务进行中

- **里程碑**：M1 ｜ **形状**：单人（solo），**工程师做**
- **拥有的文件**：`tools/verify-*.mjs`（guard/jobs/mount/pm-write-guard/rule-guard-map/tasks/links 里引用 docs/qa 的）
- **依赖**：T-126

## 要做什么

`tools/` 下所有 `docs/qa` 引用改 `qa/`：读文件路径、pin 期望字符串、tempRepo 注释、QA case 路径断言等。**只改路径，不改任何检查语义**。

## DoD（PM 写）

| # | 怎么算做完 | 别人怎么验 |
| --- | --- | --- |
| 1 | `grep -r "docs/qa" tools/` 为 0（除历史提及） | grep |
| 2 | 改过的每个 verify-*.mjs 单独跑绿 | 逐个跑 |
| 3 | `npm test` 的前半（verify-* 链）绿 | `npm test` 或分步 |
