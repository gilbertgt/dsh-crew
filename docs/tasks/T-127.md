# T-127 — package.json + CI 的 docs/qa 引用改 qa/

- **Verdicts**：code: not run — 任务进行中，评审在第 10 步 ｜ security: not run — 不动权限 ｜ qa: not run — 判据是 npm test 链能走到 run-all + CI 步骤 ｜ doc: not run — 任务进行中

- **里程碑**：M1 ｜ **形状**：单人（solo），**工程师做**
- **拥有的文件**：`package.json`、`.github/workflows/test.yml`、`.github/workflows/publish.yml`
- **依赖**：T-126

## 要做什么

- `package.json` `scripts.test`：`bash docs/qa/run-all.sh` → `bash qa/run-all.sh`。
- `.github/workflows/*.yml`：任何 `docs/qa` 引用改 `qa/`（若 workflow 直接引用 case 路径）。
- 注意：verify-mount 可能有 pin 钉 `scripts.test` 里的 `bash docs/qa/run-all.sh` 字符串（像 T-42 case-01）——改 package.json 时若 verify-mount/T-42 红，记录并留给 T-128/T-130 处理（那些 pin 也要同步改）。

## DoD（PM 写）

| # | 怎么算做完 | 别人怎么验 |
| --- | --- | --- |
| 1 | `grep -r "docs/qa" package.json .github/` 为 0 | grep |
| 2 | `scripts.test` 含 `bash qa/run-all.sh` | 读 package.json |
