# T-125 — 第 5 步：收尾验证（grep=0 + npm test 全绿）

- **Verdicts**：code: not run — 验证任务，不含产品代码 ｜ security: not run — 不动权限 ｜ qa: not run — 判据是全局 grep + npm test ｜ doc: not run — 任务进行中

- **里程碑**：M1 ｜ **形状**：单人（solo），**工程师做**
- **拥有的文件**：无新增——验证全局状态
- **依赖**：T-124

## 要做什么

最终验证整个迁移：
- `grep -r "docs/design/tasks.md" .` 为 0（除历史文档/PRD 里的提及——那些是历史记录，保留）。
- `npm test` 全绿（verify-tasks 只扫 docs/tasks/、verify-mount/rule-guard-map/pm-write-guard 都指新路径）。

## DoD（PM 写）

| # | 怎么算做完 | 别人怎么验 |
| --- | --- | --- |
| 1 | `grep -r "docs/design/tasks.md" tools/ host/ roles/ docs/qa/ docs/tasks/` 为 0（除历史提及） | grep |
| 2 | `npm test` 全绿 | `npm test` |
| 3 | `node tools/verify-tasks.mjs` 只报 docs/tasks/ 来源 | 输出 |
