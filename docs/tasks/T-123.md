# T-123 — 第 3 步：删 docs/design/tasks.md，verify-tasks 去掉 tasks.md 源

- **Verdicts**：code: not run — 任务进行中，评审在第 10 步 ｜ security: not run — 不动权限 ｜ qa: not run — 判据是 verify-tasks 只靠目录 + npm test ｜ doc: not run — 任务进行中

- **里程碑**：M1 ｜ **形状**：单人（solo），**工程师做**
- **拥有的文件**：`docs/design/tasks.md`（删）、`tools/verify-tasks.mjs`（去掉 tasks.md 源）
- **依赖**：T-122（引用已修）

## 要做什么

1. 删 `docs/design/tasks.md`。
2. 同步改 `tools/verify-tasks.mjs`：去掉它读 `docs/design/tasks.md` 的那一半（T-121 让它双源
   fail-closed，删文件后那一源会报 missing 红）。改后只读 `docs/tasks/` 目录。

## DoD（PM 写）

| # | 怎么算做完 | 别人怎么验 |
| --- | --- | --- |
| 1 | `docs/design/tasks.md` 不存在 | `ls docs/design/tasks.md` 报不存在 |
| 2 | `tools/verify-tasks.mjs` 不再读 `docs/design/tasks.md`（grep 为 0 除注释） | grep |
| 3 | verify-tasks 只靠目录仍绿（5 个 section） | `node tools/verify-tasks.mjs` |
| 4 | `npm test` 全绿 | `npm test` |
