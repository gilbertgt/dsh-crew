# T-122a — lib/qa.mjs：tempRepo 拷贝清单加 docs/tasks/，TASKS_MD 指向目录

- **Verdicts**：code: not run — 任务进行中 ｜ security: not run — 不动权限 ｜ qa: not run — 判据是 T-42 绿 + npm test ｜ doc: not run — 任务进行中

- **里程碑**：M1 ｜ **形状**：单人（solo），**工程师做**
- **拥有的文件**：`docs/qa/lib/qa.mjs`（唯一）
- **依赖**：T-121；**先于** T-122b/c/d

## 要做什么

`docs/qa/lib/qa.mjs` 的 `tempRepo()` 拷贝清单（当前含 `docs/design/tasks.md`）改成拷贝
`docs/tasks/` 目录（T-121 后 verify-tasks 读目录，case 的拷贝里必须要有，否则 T-42 红
"docs/tasks/ is missing"）。`TASKS_MD` 常量若指向旧单文件，改指目录。

## DoD

| # | 怎么算做完 | 别人怎么验 |
| --- | --- | --- |
| 1 | `tempRepo()` 拷贝清单含 `docs/tasks/`（目录）而非 `docs/design/tasks.md` | 读 qa.mjs |
| 2 | `node docs/qa/T-42/case-19-*.mjs`（任一）在拷贝里绿（不再 "docs/tasks/ is missing"） | 跑 |
| 3 | 不改任何 case 文件 | `git diff --name-only` 只有 qa.mjs |
