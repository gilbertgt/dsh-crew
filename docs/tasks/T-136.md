# T-136 — 修正 dsh-crew browser client boot graph enrollment

- **版本**：5

- **Verdicts**：code: pass (round 2) ｜ security: pass ｜ qa: not run — QA 子代理無法在目前 runtime 寫入 case（CRD 0033） ｜ doc: pass

- **里程碑**：M1 ｜ **Shape**：solo
- **擁有的檔案**：`package.json`、`cordis.patch.yml`、`host/crew.js`、`host/roles-settings.js`、`host/roles-preset.js`、`client/crew-settings.js`、`tools/verify-role-settings.mjs`、`tools/verify-client-boot-graph.mjs`、`qa/T-136/`
- **測試檔案**：`tools/verify-client-boot-graph.mjs`、`tools/verify-role-settings.mjs`、`qa/T-136/`

## 要做什麼

以最小 package boundary 修正讓 DSH Web 的 `ClientModuleRegistry` 看見 `dsh-crew`：package root `"."` export 指向既有 `./host/crew.js`，並將 `dsh-crew-core` Loader row 從 `dsh-crew/host/crew.js` 改為 bare `dsh-crew`。git-guard、pm-write-guard、row id、config contract 與 Host implementation 都保持不變。

新增不依賴 production profile 的 isolated boot-graph regression，實際解析 patch row、package root export、Host plugin 與 client bundle，並斷言 `entries`、`batches`、client bundle、唯一 core mount，以及既有 `settings.section` / 無 `settings.plugin.item` 語意。

## DoD

- package root export `"."` 存在，解析結果是既有 `host/crew.js`；`"./client"` 仍指向 `client/crew-settings.js`。
  - 他人驗證：`node tools/verify-client-boot-graph.mjs`。
- `dsh-crew-core` row 的 `name` 是 bare `dsh-crew`，三個 host rows 仍各只出現一次，且 id/config contract 不變。
  - 他人驗證：`node tools/verify-client-boot-graph.mjs`。
- isolated composed boot graph 含有 `dsh-crew` entry 與至少一個包含它的 batch，該 graph row 指向 package 的 `./client` bundle；將 row 改回 subpath 時測試必須失敗，不能只靠 grep 通過。
  - 他人驗證：`node tools/verify-client-boot-graph.mjs`。
- `settings.section` 的 `id: "crew"`、`order: 30`、`dsh-crew-roles` 與 `settings.plugin.item` 不存在的既有契約仍通過。
  - 他人驗證：`node tools/verify-client-boot-graph.mjs` 與 `node tools/verify-role-settings.mjs`。
- 所有既有驗證、`bash qa/run-all.sh`、`npm.cmd test`、`git diff --check` 通過；optional role-tool mount 缺少時只保留既有 skip。
  - 他人驗證：依 PRD 的完整命令執行。
- 已完成一輪 code、security、QA、doc review；未完成的 review 會保留在 Verdicts 的實際狀態，不以文字假設通過。
  - 他人驗證：讀取本 task 的 Verdicts 與各 review report。

## 文件補充（2026-09-15；保留上方 v1 已確認文字）

### 已回報的問題

- **回報者**：PM，依 DSH Web `ClientModuleRegistry` 的 browser boot-graph failure 整理。
- **重現命令**：`node tools/verify-client-boot-graph.mjs`；負向 input 是 temporary copy 中的 active `dsh-crew-core` row，將 `name: 'dsh-crew'` 改回 `name: 'dsh-crew/host/crew.js'`。
- **實際觀察**：subpath row 無法讓 `dsh-crew` 出現在 composed `entries` 或 `batches`，既有 `settings.section` 因而無法進入瀏覽器載入圖。
- **預期結果**：bare `dsh-crew` row 應解析既有 Host plugin 與 `./client` bundle，composed graph 應包含 `dsh-crew` entry 及包含它的 batch；subpath mutation 應讓 verifier 以 named `FAIL` 非零結束。
- **限制**：production `dsh.cmd web`、production profile 與真實 `C:\Users\gilbe\.dsh` 均不在本作業執行範圍。

### DoD 澄清

下列是本 task 的可核對版本；上方原始 DoD 文字保留。原 DoD item 3 的兩個獨立條件在此拆開，並重新對齊後續 item 編號；這是語意澄清，不新增產品行為。

- **item 1**：package root export `"."` 存在且解析到既有 `host/crew.js`；`"./client"` 仍解析到 `client/crew-settings.js`。由 `node tools/verify-client-boot-graph.mjs` 核對。
- **item 2**：`dsh-crew-core` row 的 `name` 是 bare `dsh-crew`；三個 host rows 的 id、name、config contract 與唯一 core mount 保持正確。由 `node tools/verify-client-boot-graph.mjs` 核對。
- **item 3**：isolated composed boot graph 包含 `dsh-crew` entry、包含它的 batch，且 graph row 指向 package 的 `./client` bundle。由 `node tools/verify-client-boot-graph.mjs` 與 `node qa/T-136/case-03-composed-boot-graph.mjs` 核對。
- **item 4**：將 `dsh-crew-core` row 改回 `dsh-crew/host/crew.js` 時，real verifier 必須以 named `FAIL` 非零結束，不能只靠 grep 通過。由 `node qa/T-136/case-04-subpath-mutation-red.mjs` 核對。
- **item 5**：`settings.section` 的 `id: "crew"`、`order: 30`、`dsh-crew-roles` 與不存在 `settings.plugin.item` 的既有契約仍通過。由 `node tools/verify-client-boot-graph.mjs` 與 `node tools/verify-role-settings.mjs` 核對。
- **item 6**：所有既有驗證、QA runner、完整測試與 diff check 通過；執行 `node tools/verify-guard.mjs`、`node tools/verify-pm-write-guard.mjs`、`node tools/verify-rule-guard-map.mjs`、`node tools/verify-jobs.mjs`、`node tools/verify-role-settings.mjs`、`node tools/verify-client-boot-graph.mjs`、`node tools/verify-mount.mjs`、`node tools/verify-preset-install.mjs`、`node tools/verify-links.mjs`、`node tools/verify-tasks.mjs`、`bash qa/run-all.sh`、`npm.cmd test` 與 `git diff --check`；optional role-tool mount 缺少時只保留既有 skip。`bash` 在本 Windows runtime 以 `C:\Program Files\Git\bin\bash.exe` 執行。
- **item 7**：PM 逐一核對 `code`、`security`、`qa`、`doc` 四份 review evidence 與本 task 的 Verdicts；任何尚未取得的 review 必須保留 `not run — <reason>`，不能以 QA case 的 manual-gate guard 冒充 pass。現階段 review report 沒有 repository-local path、format 或 repeatable command，該限制持續記錄於 `qa/gaps.md` section 63；核對方式是讀取本 task 的 Verdicts、各 role 的本輪 report，以及 `node tools/verify-tasks.mjs` 對 Verdicts 格式的結果。
- **item 8（CRD 0032）**：`dsh-crew-roles` settings scope 尚未 ready 或 unavailable 時，Crew section 不可回傳空白；必須顯示頁面標題、九個 role rows 與不可用狀態，且控制項維持 disabled。由 `node tools/verify-role-settings.mjs` 核對 ready 與 unavailable 兩種 render。
- **2026-09-15 修正（CRD 0033）**：本次不新增 item 8 的 QA case；改以獨立 QA 的 case 規格審查，以及 `tools/verify-role-settings.mjs` 的 test-first 紅燈與綠燈結果核對 item 8。`qa` verdict 必須保持 `not run — QA 子代理無法在目前 runtime 寫入 case（CRD 0033）`；code、security、doc review 與完整測試仍照原標準執行。

## Corrections

- 2026-09-15，v3 → v4（CRD 0032）：將 CRD 已列出的 runtime、client、regression 與 QA 路徑加入本 task 的 ownership 與測試檔案，並把先前完成但已被重新開啟之 review verdicts 改回實際的 `not run` 狀態。原有 DoD 與 CRD 0032 新增的 item 8 均保留。
- 2026-09-15，v4 → v5（CRD 0033）：使用者接受本次 QA runtime 例外。兩個獨立 `crew_qa` 均因 approval prompts disabled，且 repository QA case 寫入被 pm-write-guard 當成 PM 寫入而拒絕，因此本次不新增 item 8 的 QA case；改以獨立 QA 的 case 規格審查，以及 `tools/verify-role-settings.mjs` 的 test-first 紅燈／綠燈結果核對 item 8。`qa` verdict 必須保持 `not run — QA 子代理無法在目前 runtime 寫入 case（CRD 0033）`，不可冒充 pass；code、security、doc review 與完整測試仍照原標準執行。
