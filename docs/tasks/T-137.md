# T-137 — merge 前最後修正：reviewRounds 與 direct／solo／crew 路由

- **版本**：1

- **Verdicts**：code: not run — 使用者指示以單一 Agent 直接完成修正，未啟動 Crew、子代理或 review 流程 ｜ security: not run — 同上，本次只改 prompt、runtime 預設與 QA，未啟動 security review ｜ qa: pass — 新增 `qa/T-137/` 三個 case 並通過 `bash qa/run-all.sh` 與 `npm test` ｜ doc: not run — 未啟動 doc review，改以 `npm test` 既有文本檢查與 `node tools/verify-links.mjs` 核對

- **里程碑**：M1 ｜ **Shape**：solo
- **擁有的檔案**：`roles/pm.md`、`host/crew.js`、`cordis.patch.yml`、`tools/verify-mount.mjs`、`qa/T-07/`、`qa/T-137/`、`principles.md`、`README.md`、`README-zh.md`、`CONTRIBUTING.md`、`CLAUDE.md`
- **測試檔案**：`qa/T-137/`、`qa/T-07/case-02-default-limits.mjs`、`qa/T-07/case-05-cordis-patch-comment.mjs`、`tools/verify-mount.mjs`

## 要做什麼

在 `crew/browser-client-boot-graph` 分支合併前完成三項最小修正：

1. `reviewRounds` 的 runtime 預設與所有相關範例由 `3` 改為 `2`，與「初次 review ＋ 修正後一次 re-check，禁止第三輪」一致。
2. PM routing 不再把「任何 user input」或一般低風險 coding 條件視為必須完整 Crew；「是否需要 security review」與「是否需要完整 Crew」拆成兩個判斷。
3. 新增 `solo` 路由（PM → 單一 engineer），並加入實際 routing decision table 與其可執行 QA。

不處理 checkpoint host enforcement、linked dependency 與 GitHub Actions CI。

## DoD

- **item 1**：`reviewRounds` 的 runtime 預設、`cordis.patch.yml` 範例、PM prompt、`roles/pm.md` 的文字與三個 reviewer persona 一致為兩輪，且沒有任何一處仍宣稱三輪或第三輪。
  - 他人驗證：`node qa/T-137/case-01-review-rounds-two-everywhere.mjs`。
- **item 2**：`roles/pm.md` step 1 帶有一張四欄 routing decision table，且七列分別為 README 拼字 → `direct`、單檔純函式 bug → `direct`、3 個一般 product files → `solo`、普通設定 UI → `solo`、UI + auth/permission → `solo` ＋ security review、跨核心模組 contract 變更 → `crew`、migration + auth + network → `crew`。
  - 他人驗證：`node qa/T-137/case-02-routing-decision-table.mjs`。
- **item 3**：crew 觸發條件不再包含「任何 user input」；`roles/pm.md` 明說只接受使用者輸入的畫面不是風險變更；step 10b 的 security 清單改用 trust boundary 措辭，且兩個判斷有明確的先後順序。
  - 他人驗證：`node qa/T-137/case-03-security-review-separate-from-route.mjs`。
- **item 4**：`host/crew.js` 的 runtime 預設改為 2 之後，`node tools/verify-mount.mjs` 與 `qa/T-07` 仍綠燈，且 `npm test` 中的既有 boot graph 檢查沒有回歸。
  - 他人驗證：`node tools/verify-mount.mjs`、`node tools/verify-client-boot-graph.mjs`、`node qa/T-136/case-03-composed-boot-graph.mjs`。
- **item 5**：既有 QA 全數通過，且 `npm test` 全綠。
  - 他人驗證：`bash qa/run-all.sh`、`npm test`。

## 範圍

- 本次沒有啟動 Crew、子代理或額外 review 流程，因此 code／security／doc 三項 verdict 保留 `not run` 與理由，不以文字假設通過（`node tools/verify-tasks.mjs` 僅驗證該行被寫下且每個 skip 帶理由）。
- routing 沒有新增獨立的程式化 router：現況仍由 `roles/pm.md` 驅動，因此 item 2 與 item 3 的 QA 對 shipped PM prompt 做可執行的解析與負向斷言，而非另建一套重複的 runtime。
