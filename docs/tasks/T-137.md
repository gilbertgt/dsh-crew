# T-137 — merge 前最後修正：reviewRounds 與 direct／solo／crew 路由

- **版本**：4

- **Verdicts**：code: not run — 使用者指示以單一 Agent 直接完成修正，未啟動 Crew、子代理或 review 流程 ｜ security: not run — 同上，本次只改 prompt、runtime 驗證與 QA，未啟動 security review ｜ qa: pass — `qa/T-137/` 五個 case 全綠，`bash qa/run-all.sh` 40/40 且 `npm test` exit 0 ｜ doc: not run — 未啟動 doc review，改以 `npm test` 既有文本檢查與 `node tools/verify-links.mjs` 核對

- **里程碑**：M1 ｜ **Shape**：solo
- **擁有的檔案**：`roles/pm.md`、`host/crew.js`、`cordis.patch.yml`、`tools/verify-mount.mjs`、`qa/T-07/`、`qa/T-136/case-06-full-verification.mjs`、`qa/T-137/`、`qa/rule-guard-map.md`、`qa/gaps.md`、`principles.md`、`README.md`、`README-zh.md`、`CONTRIBUTING.md`、`CLAUDE.md`
- **測試檔案**：`qa/T-137/`、`qa/T-07/case-02-default-limits.mjs`、`qa/T-07/case-04-bad-limit-still-throws.mjs`、`qa/T-07/case-05-cordis-patch-comment.mjs`、`tools/verify-mount.mjs`

## 要做什麼

在 `browser-client-boot-graph` 分支合併前完成最小修正：

1. `reviewRounds` 的 runtime 預設與所有相關範例由 `3` 改為 `2`，與「初次 review ＋ 修正後一次 re-check，禁止第三輪」一致。
2. PM routing 不再把「任何 user input」或一般低風險 coding 條件視為必須完整 Crew；「是否需要 security review」與「是否需要完整 Crew」拆成兩個判斷。
3. 新增 `solo` 路由（PM → 單一 engineer），並加入實際 routing decision table 與其可執行 QA。
4. 移除會覆蓋 `direct` 的無條件舊規則，簡化 `solo` 的儀式化流程，把 `reviewRounds` 收成硬上限，並讓 `CONTRIBUTING.md` 與三種路由一致。
5. 三條流程真正分離（`direct`／`solo`／`crew` 各有自己的流程），ADR 與 CRD 只屬於 `solo`／`crew`，`reviewRounds` 只接受 `2` 或未設定。

不處理 checkpoint host enforcement、linked dependency 與 model routing、權限、Git guard、PM write guard、繁中政策或 boot graph。

## DoD

- **item 1**：`reviewRounds` 的 runtime 預設、`cordis.patch.yml` 範例、PM prompt、`roles/pm.md` 的文字與三個 reviewer persona 一致為兩輪，且沒有任何一處仍宣稱三輪或第三輪。
  - 他人驗證：`node qa/T-137/case-01-review-rounds-two-everywhere.mjs`。
- **item 2**：`roles/pm.md` step 1 帶有一張四欄 routing decision table，且七列分別為 README 拼字 → `direct`、單檔純函式 bug → `direct`、3 個一般 product files → `solo`、普通設定 UI → `solo`、UI + auth/permission → `solo` ＋ security review、跨核心模組 contract 變更 → `crew`、migration + auth + network → `crew`。
  - 他人驗證：`node qa/T-137/case-02-routing-decision-table.mjs`。
- **item 3**：crew 觸發條件不再包含「任何 user input」；`roles/pm.md` 明說只接受使用者輸入的畫面不是風險變更；step 10b 的 security 清單改用 trust boundary 措辭，且兩個判斷有明確的先後順序。
  - 他人驗證：`node qa/T-137/case-03-security-review-separate-from-route.mjs`。
- **item 4**：`host/crew.js` 的 runtime 預設改為 2 之後，`node tools/verify-mount.mjs` 與 `qa/T-07` 仍綠燈，且 `npm test` 中的既有 boot graph 檢查沒有回歸。
  - 他人驗證：`node tools/verify-mount.mjs`、`node tools/verify-client-boot-graph.mjs`、`node qa/T-136/case-03-composed-boot-graph.mjs`。
- **item 5**：既有 QA 全數通過，且 `npm test` 全綠。
  - 他人驗證：`bash qa/run-all.sh`、`npm test`。
- **item 6**：整份 PM prompt 不再有會覆蓋 `direct` 不變規則的無條件舊規則——`direct` 明列跳過 interview、PRD、HLD、ADR、CRD、design document、task row、team flow、QA 與 review，bug 段落、step 2、step 4 與 Hard rules 的 PRD／ADR／CRD 條目都各自標明所屬 route，且沒有任何「單位」同時具備「every change 級量化詞＋必要產物」卻不點名 route；`CONTRIBUTING.md` 也改為三種路由的表格。
  - 他人驗證：`node qa/T-137/case-05-direct-invariants.mjs`。
- **item 7**：`limits.reviewRounds` 只接受 `2` 或未設定；`0`、`1`、`3`、`4`、`99`、`2.5` 及字串 `"2"` 都在 mount 時被拒絕，錯誤訊息點名 `limits.reviewRounds`、指出唯一合法值 2 並說明可以省略；`2` 與未設定皆可 mount，且 prompt 以「fixed, not a preference」呈現這個數字；`liveAgents` 不受此限。
  - 他人驗證：`node qa/T-137/case-04-review-rounds-cap.mjs`、`node qa/T-07/case-04-bad-limit-still-throws.mjs`。
- **item 8**：`cordis.patch.yml` 的註解、`roles/pm.md` 的 Hard rules、`principles.md` 指向 bug 段落的引文與 `qa/rule-guard-map.md` 的條目都跟得上新的路由與上限措辭，`node tools/verify-rule-guard-map.mjs` 與 `node tools/verify-links.mjs` 綠燈。
  - 他人驗證：`node tools/verify-rule-guard-map.mjs`、`node tools/verify-links.mjs`。
- **item 9**：三條流程真正分離。step 1 寫出 `direct`／`solo`／`crew` 各自的內容，`solo` 有自己五項流程（讀 repo → 最多一個問題 → task row／DoD → 一個 engineer → targeted test 與 completion gate → commit），並明說 numbered flow 的步驟不得加回它；原本的步驟清單標題改為 `## The \`crew\` flow, step by step`，開頭聲明只屬於 `crew` 並點名 `solo` 借用的兩個步驟（step 9 的 briefing、step 11 的 commit）；`solo` 可啟動的角色上限寫成「那一位 engineer 加上 step 1 指名的那一位 reviewer」；step 2、3、4、5、6、9、10、12、18、state file 與 CRD／ADR 兩節都補上 route 歸屬。
  - 他人驗證：`node qa/T-137/case-05-direct-invariants.mjs`。
- **item 10**：ADR 與 CRD 只適用 `solo`／`crew`。`direct` 不建立 PRD、HLD、ADR、CRD、design document、task row、QA 或 review 文件；小型實作選擇留在程式與 commit 說明，需要獨立記錄的決策則重新 route 成 `solo` 或 `crew`。`solo` 的決策紀錄不上升成 `crew`：需要記錄時由 PM 自己寫 ADR（該 route 沒有 architect），改動 task row 的 DoD section 則寫成 CRD。兩節標題點名 route，`direct` 的 bullet 明列跳過 ADR／CRD／design document，Hard rules 的 ADR 與 CRD 條目也各自標明 route，且 `roles/pm.md`、`CLAUDE.md`、`CONTRIBUTING.md`、`principles.md` 四份文件對 `solo` 的 ADR 歸屬必須同一答案。
  - 他人驗證：`node qa/T-137/case-05-direct-invariants.mjs`。

## 範圍

- 本次沒有啟動 Crew、子代理或額外 review 流程，因此 code／security／doc 三項 verdict 保留 `not run` 與理由，不以文字假設通過（`node tools/verify-tasks.mjs` 僅驗證該行被寫下且每個 skip 帶理由）。
- routing 沒有新增獨立的程式化 router：現況仍由 `roles/pm.md` 驅動，因此 item 2、3、6、9、10 的 QA 對 shipped PM prompt 做可執行的解析與負向斷言，而非另建一套重複的 runtime。
- `reviewRounds` 的界線做成專屬的 `reviewRoundsOf()`（`limitOf` 回到原本的三參數範圍檢查），沒有新增設定欄位，也沒有改動 `liveAgents` 的語意。

## 同日追加（review 回合，v1 → v2）

- 使用者要求補做四件事，全部落在同一組檔案：`direct` 不變規則（item 6）、`solo` 簡化（item 6 涵蓋的 prompt 段落）、`reviewRounds` 硬上限（item 7）、`CONTRIBUTING.md` 三路由一致（item 6 的後半）。item 1 至 item 5 保留原樣並繼續驗證。
- 新增 `qa/T-137/case-04-review-rounds-cap.mjs`、`qa/T-137/case-05-direct-invariants.mjs`；`qa/T-07/case-04-bad-limit-still-throws.mjs` 的合法值由 `reviewRounds: 9` 改為上限 `2`；`qa/T-137/case-01` 的 `cordis.patch.yml` 行篩選收緊為真正賦值的那一行。
- `roles/pm.md` 的 bug 段落更名為 `## A bug becomes a task row — on the \`crew\` and \`solo\` routes only`，因此 `principles.md` 的引文與 `qa/rule-guard-map.md` 的條目在同一次修正中跟著更新（`node tools/verify-rule-guard-map.mjs` 要求該條目是原始檔某一行的逐字子字串）。

## 同日追加（第二次 review 回合，v2 → v3）

- 使用者指出三項 blocker：(1) 編號步驟仍是所有 route 共用、(2) `direct` 與 ADR／CRD 規則衝突、(3) `reviewRounds` 允許 `1`。逐條修正成 item 9 與 item 10，並把 item 7 收緊為「只接受 2 或未設定」。
- `roles/pm.md`：step 1 新增 `solo` 五項流程；步驟清單更名並加上 `crew` 專屬聲明；step 2、3、5、6、9、10、12、18、state file、CRD 節、ADR 節、Hard rules（ADR／CRD／milestone 三條）都補上 route 歸屬；`direct` bullet 加上 ADR／CRD／design document 三項跳過。
- `host/crew.js`：`limitOf` 回到範圍檢查，`reviewRounds` 改由 `reviewRoundsOf()` 驗證「只能是 2 或未設定」，錯誤訊息說明唯一合法值與可省略。
- 測試：`qa/T-137/case-04` 改寫成 7 個拒絕值＋2 個接受形；`qa/T-137/case-05` 加入 ADR／CRD／design document 的防漏（三項新 mutation、段落切成 bullet 級單位，`direct` 與 `solo` 流程／`crew` 流程聲明），並修正 `CONTRIBUTING.md` 上限措辭的檢查；`qa/T-07/case-04` 改成每個 limit 各自的訊息期望。
- 一致性：`CONTRIBUTING.md`、`CLAUDE.md`、`principles.md` 的 ADR／CRD 與路由敘述同步收斂到 `solo`／`crew`。

## 同日追加（第三次 review 回合，v3 → v4）

- 使用者要求 review 這一批未提交的 diff。Review 找到三組互相矛盾的敘述，這一輪逐條修掉；方向依原本規格（ADR 屬 `solo`／`crew`、`direct` 不寫），因此修的是主張「`solo` 不寫 ADR」的那一側。
- **(1) `solo` 的決策紀錄**：`roles/pm.md` 的 solo flow 原本寫「never writes an ADR or a CRD for a small implementation choice… moves the work up to `crew`」，與 ADR 節（`solo` 是 ADR route）以及 `CONTRIBUTING.md` 的表格互相衝突，`CLAUDE.md` 與 `principles.md` 也各自寫成「neither `solo` nor `direct` … writes an ADR or a CRD」。現在四份文件同一答案：需要記錄時由 PM 自己寫 ADR（`solo` 沒有 architect），改動 task row 的 DoD section 則寫成 CRD。
- **(2) `solo` 可啟動的角色**：`roles/pm.md` 有三處說 `solo` 完全不啟動 QA／reviewer（含步驟清單聲明的「starts an architect, QA or a reviewer」），但 step 1 的 routing decision table 與 step 10 的例外都允許「step 1 指名的那一位 reviewer」。三個說法改成「那一位 engineer 加上 step 1 指名的那一位 reviewer，不能再多」。
- **(3) 步驟歸屬**：`solo` 自己說要「with step 9's briefing list」與「commit (step 11)」，但步驟清單聲明只提 `direct` 的 commit 例外。聲明改為點名 `solo` 借用的兩個步驟。
- 另外把「never keeps a milestone」統一成「keeps no milestone of its own」，避免與 Hard rules 的「Every change gets a milestone… on `solo` and `direct` the milestone is the change itself」互斥；`CONTRIBUTING.md` 重複兩次的「only `crew` runs the numbered steps」也收成一次。
- 測試：`qa/T-137/case-05` 從 45 增到 61 checks——新增 `soloReviewer`（單一具名 reviewer）、`soloAdr`（決策留在 `solo`）、`crewFlow` 需點名兩個借用步驟，以及跨四份文件的 `auditDocuments()`（ADR 必須在 `solo`／`crew`，且任何文件都不得否認 `solo` 有決策紀錄）；banned list 加入這三組舊措辭，mutation 從 9 增到 12（含「一份文件否認 `solo` 有 ADR」的 in-memory mutation）。
