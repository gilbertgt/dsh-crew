# T-122d — rule-guard-map 的 DoD 来源改指新位置

- **Verdicts**：code: not run — 任务进行中 ｜ security: not run — 不动权限 ｜ qa: not run — 判据是 verify-rule-guard-map 绿 + npm test ｜ doc: not run — 任务进行中

- **里程碑**：M1 ｜ **形状**：单人（solo），**工程师做**
- **拥有的文件**：`docs/qa/rule-guard-map.md`、`tools/verify-rule-guard-map.mjs`（只改对 DoD 来源的解析）
- **依赖**：T-121

## 要做什么

`docs/qa/rule-guard-map.md` 里对 DoD 来源的 section（如 `### docs/design/tasks.md (DoD)`）
改指新位置（`docs/tasks/README.md (DoD)` 或 `docs/tasks/`）。`tools/verify-rule-guard-map.mjs`
里解析该来源的代码同步改。**不改 rule-guard-map 的规则行和守卫状态，只改来源位置**。

## DoD

| # | 怎么算做完 | 别人怎么验 |
| --- | --- | --- |
| 1 | `grep -r "docs/design/tasks.md" docs/qa/rule-guard-map.md tools/verify-rule-guard-map.mjs` 为 0（除历史提及） | grep |
| 2 | `node tools/verify-rule-guard-map.mjs` 绿 | 跑 |
| 3 | 规则行和守卫状态不变 | 抽查 diff |

## PM 对 Q-01 的决定（2026-08-23，写进任务行）

采纳**选项 A**：
1. **来源**改 `### docs/tasks/ (DoD)`（目录形式）；`tools/verify-rule-guard-map.mjs` 的 SECTION 正则
   接受目录路径，来源是目录时读目录下所有 `.md` 文件，bare 锚点对所有文件行做子串检查，existsSync
   对目录通过。
2. **"规则行不变"**读作：规则含义、owner、status 不变；锚点文本随来源迁移重指（锚点来源绑定，
   属"只改来源位置"）。
3. **T-01 重复行**：删（architect section 第 72 行已有同一规则，删除不损失覆盖；目录里无锚点，留则必红）。
4. **`docs/qa/<task-id>/` 锚点**：用 `docs/qa/T-*/case-*.mjs`（目录实际形状）。
5. 其余 bare 锚点重指：`DoD section` → `## DoD`（目录里 9 处）、`docs/design/tasks.md` →
   `docs/tasks/`（README 第 3 行）、`solo` 不变（目录里 9 处）。计数行按实际改（预计 7→6）。
6. intro 和 section 的注释（第 38、397 行）同步改指新来源。
