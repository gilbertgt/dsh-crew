# T-124 — 第 4 步：扫尾（verify-mount pin、pm-write-guard 白名单、rule-guard-map 来源）

- **Verdicts**：code: not run — 任务进行中，评审在第 10 步 ｜ security: not run — pm-write-guard 白名单改目录，守卫逻辑不变 ｜ qa: not run — 判据是各测试绿 + npm test ｜ doc: not run — 任务进行中

- **里程碑**：M1 ｜ **形状**：单人（solo），**工程师做**
- **拥有的文件**：`tools/verify-mount.mjs`（pin 的期望字符串）、`host/pm-write-guard.js`（白名单）、`tools/verify-pm-write-guard.mjs`（白名单测试）、`docs/tasks/README.md`（如实提及历史）
- **依赖**：T-123（rule-guard-map 来源已由 T-122d 处理，本任务不再拥有 `tools/verify-rule-guard-map.mjs`）

## 要做什么

- `verify-mount.mjs` 要求角色提示含 `docs/design/tasks.md` 的 pin（line ~1212-1215、~1572、~1587）→ 改成要求 `docs/tasks/`。
- `host/pm-write-guard.js` 白名单 `docs/design/tasks.md` → `docs/tasks/`（目录，PM 可写）。
- 同步 `verify-pm-write-guard.mjs` 白名单断言。
- 同步 `docs/tasks/README.md` 里对"前身是 tasks.md"的表述（如实保留历史提及）。
- （rule-guard-map 的 DoD 来源已由 T-122d 改为 `docs/tasks/ (DoD)`，本任务不碰。）

## DoD（PM 写）

| # | 怎么算做完 | 别人怎么验 |
| --- | --- | --- |
| 1 | `grep -r "docs/design/tasks.md" tools/ host/` 为 0（除注释提及） | grep |
| 2 | `node tools/verify-mount.mjs` 绿 | 直接跑 |
| 3 | `node tools/verify-pm-write-guard.mjs` 绿 | 直接跑 |
| 4 | `node tools/verify-rule-guard-map.mjs` 绿 | 直接跑 |
| 5 | `npm test` 全绿 | `npm test` |
