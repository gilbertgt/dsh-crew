# T-132 — host/ + tools/lib/ 的 docs/qa 引用改 qa/

- **Verdicts**：code: not run — 任务进行中，评审在第 10 步 ｜ security: not run — pm-write-guard 白名单改路径，守卫逻辑不变 ｜ qa: not run — 判据是 verify-pm-write-guard 绿 + npm test ｜ doc: not run — 任务进行中

- **里程碑**：M1 ｜ **形状**：单人（solo），**工程师做**
- **拥有的文件**：`host/pm-write-guard.js`、`host/roles.js`、`tools/lib/boot-log.mjs`
- **依赖**：T-128（verify-pm-write-guard 的 fixture 已改 qa/，白名单不跟就会红）

## 要做什么

T-128 发现 `host/` 和 `tools/lib/` 不在任何任务行里，但有 `docs/qa` 引用：
- `host/pm-write-guard.js` line 172-173：**运行时 whitelist** `["docs","qa","run-all.sh"]` / `["docs","qa","gaps.md"]` → `["qa","run-all.sh"]` / `["qa","gaps.md"]`（否则 verify-pm-write-guard 的 qa/run-all.sh、qa/gaps.md fixture 被拦）。line 40-41 注释同步。
- `host/roles.js` line 136：`summary: "Test the result, with cases in docs/qa/"` → `qa/`（运行时字符串）；line 133 注释同步。
- `tools/lib/boot-log.mjs` line 12/19：2 处注释 `docs/qa` → `qa`。

只改路径，不改任何守卫/角色逻辑。

## DoD（PM 写）

| # | 怎么算做完 | 别人怎么验 |
| --- | --- | --- |
| 1 | `grep -r "docs/qa" host/ tools/lib/` 为 0（除历史提及） | grep |
| 2 | `node tools/verify-pm-write-guard.mjs` 绿（原 2 红消失） | 跑 |
| 3 | `node tools/verify-mount.mjs` 绿（roles.js summary 改动不破坏） | 跑 |
| 4 | `npm test` 前半（verify-* 链）绿 | 跑 |
