# T-122c — 9 个角色提示的路径引用改指 docs/tasks/

- **Verdicts**：code: not run — 任务进行中 ｜ security: not run — 不动权限 ｜ qa: not run — 判据是 verify-mount 绿 + npm test ｜ doc: not run — 任务进行中

- **里程碑**：M1 ｜ **形状**：单人（solo），**工程师做**
- **拥有的文件**：引用 `docs/design/tasks.md` 的 9 个 `roles/*.md`
- **依赖**：T-121

## 要做什么

把 9 个角色提示里对 `docs/design/tasks.md` 的引用改成 `docs/tasks/`（目录）。**只改路径
字符串，不改角色提示的任何规则语义**。注意：`tools/verify-mount.mjs` 有 pin 要求角色提示
包含某个任务表路径字符串（那是 T-124 的活，它会同步改 pin）——本任务只改角色提示这一侧，
改完 verify-mount 可能红，那是预期的中间态，T-124 会修 pin。

## DoD

| # | 怎么算做完 | 别人怎么验 |
| --- | --- | --- |
| 1 | `grep -r "docs/design/tasks.md" roles/*.md` 为 0（除历史提及） | grep |
| 2 | 每个角色提示只改路径，规则语义一字不变 | 抽查 diff |
| 3 | （verify-mount 可能红，记录并留给 T-124） | — |
